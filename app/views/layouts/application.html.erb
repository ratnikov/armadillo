<!DOCTYPE html>
<html>
<head>
  <title>Armadillo</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>

  <ul id="flash">
    <% flash.each do |type, message| -%>
      <%= content_tag :li, h(message), :class => h(type) %>
    <% end -%>
  </ul>

  <ul id="nav">
    <li><%= link_to t('user.create_link'), new_user_path %></li>
  </ul>

  <%= yield %>

</body>
</html>
