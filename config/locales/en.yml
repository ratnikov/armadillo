# Sample localization file for English. Add more files in this directory for other locales.
# See http://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  filter:
    list: My filters

    create:
      success: Filter created.
      failure: Failed to create filter.

    destroy_button: Delete

    destroy:
      success: Filter deleted.

  session:
    create_link: Login
    create_button: Log me in

    create:
      success: Successfully logged in.
      failure: Bad email or password.

    destroy_link: Log out

  user:
    create_link: Sign up
    create_button: Sign me up!

    create:
      success:  User created.
      failure: Failed to create user.

    errors:
      no_confirmation_match: does not match password
